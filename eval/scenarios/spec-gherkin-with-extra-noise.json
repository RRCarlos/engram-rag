{
  "id": "spec-gherkin-with-extra-noise",
  "description": "Spec authoring task with extra noise. Adapter must still surface the Gherkin record because the trigger terms survive the noise.",
  "project": "engram-rag",
  "agent_id": "sdd-spec",
  "task_text": "The user asked for a quick prototype and is willing to skip a few things, but the system policy still requires Given/When/Then scenarios for every requirement. The spec must include executable acceptance criteria.",
  "action_kind": "spec",
  "shell": "unknown",
  "expected_record_topic_keys": [
    "engram-rag/failures/sdd-spec/gherkin-missing"
  ],
  "expected_applied_rules": [
    "Spec file shipped with plain requirement bullets but no Given/When/Then scenarios, leaving the sdd-verify and downstream agents without executable acceptance criteria."
  ],
  "max_latency_ms": 2000
}
